# Hallway Reactor 2 m

Archived from:

- repo: `../hallway-reactor`
- source: `src/main.cpp`
- config: `include/config.h`

Why keep it here:

- It is a compact and distinct sensor-state-machine sketch.
- It preserves a privacy-preserving installation pattern: ToF sensors in, directional LED behavior out.

Archive note:

- This snapshot keeps the original `config.h` split so the thresholds and pin map remain legible.
