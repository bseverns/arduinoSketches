# Horizon Preset Morph

Archived from:

- repo: `../horizon`
- source: `examples/preset_morph/preset_morph.ino`

Why keep it here:

- It is a strong self-running demo surface that shows off smoothing and scene contrast without extra controls.
- It is a better showcase candidate than the minimal wrapper because it explains the sonic intent.
