# MOARkNOBS Button + Envelope USB MIDI Demo

Archived from:

- repo: `../MOARkNOBS-42`
- source: `firmware/test/demo_button_ef_usb_midi.cpp`

Why keep it here:

- It is a compact controller sketch with a clear hardware story.
- It captures the later Teensy USB-MIDI control-surface phase that the main sketchbook barely shows yet.

Archive note:

- This snapshot was lightly adapted to use direct `analogRead()` and `digitalRead()` calls so it stands alone without the MOARkNOBS hardware shim.
