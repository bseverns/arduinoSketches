# MotorLightSound Lane Controller

Archived from:

- repo: `../MotorLightSound`
- source: `lanes/lane-controller/lane-controller.ino`

Why keep it here:

- It captures a distributed-controller pattern that the main sketchbook does not currently show.
- It is a compact mix of ToF sensor bring-up, group logic, serial command dispatch, and ack handling.

Archive note:

- This is a direct source snapshot preserved as a lane-side controller sketch.
