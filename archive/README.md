# Archive

This folder keeps older branches, final snapshots, or historical builds that should remain available without crowding the active repo structure.

Use `archive/` when the work is worth preserving but no longer belongs in the main experiment or project flow.
