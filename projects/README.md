# Projects

This folder is for builds that read less like isolated sketches and more like cohesive devices or installations.

Current domains:

- `interactive-installs/`
- `personal-builds/`

Use `projects/` when a line of work has moved beyond experimentation and represents a more complete system.
