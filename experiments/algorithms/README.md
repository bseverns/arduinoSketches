# Algorithms

This domain is for sketches whose main identity is the generative or procedural idea itself.

Current members:

- `generativeAlgoSound/`
- `oneLineAlgo/`
- `_2noise/`

Rule of thumb:

- Put algorithm-first studies here.
- Leave hardware-led sketches such as `glitchamin`, `Musicator`, or `SimpleZAR` elsewhere unless they form a clearer family later.
